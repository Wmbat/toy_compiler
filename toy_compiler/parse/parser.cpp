#include <toy_compiler/parse/parser.hpp>

#include <libcaramel/containers/dynamic_array.hpp>

namespace parse
{
   void parse_items(std::span<lex::item> items)
   {
      [[maybe_unused]] auto item_it = std::begin(items);
   }
} // namespace parse
